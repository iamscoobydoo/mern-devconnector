import React from "react";

const Loading = () => {
    return (
        <>
            <i style={{ margin: "auto" }} className='fa fa-spinner fa-spin fa-3x'></i>
        </>
    );
};

export default Loading;
